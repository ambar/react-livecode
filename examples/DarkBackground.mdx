---
name: Dark Background
route: '/dark-background'
---

import {chunk} from 'lodash'
import LiveCode from '../src/LiveCode'
import 'prismjs/themes/prism.css'

<div
  style={{
    margin: '0 auto',
    maxWidth: '980px',
  }}
>
  <style>
    {`
    #root > div {
      height: 100%;
    }
    #root > div > a {
      color: #eee;
    }
    #root > div > div:last-child {
      background: #1d2330;
    }
    `}
  </style>
  <LiveCode
    autoFocus
    style={{
      height: '80vh',
    }}
    scope={{chunk}}
    code={`
      chunk(['a', 'b', 'c', 'd'], 2)
      new Map([['key1', 'value1'], ['key2', 'value2']])
    `}
  />
</div>
